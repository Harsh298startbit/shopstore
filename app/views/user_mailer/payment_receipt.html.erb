<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #28a745; color: white; padding: 20px; text-align: center; }
    .content { background-color: #f9f9f9; padding: 20px; }
    .receipt-box { background-color: white; padding: 20px; margin: 20px 0; border-radius: 5px; border: 2px solid #28a745; }
    .payment-info { background-color: #e8f5e9; padding: 15px; border-radius: 5px; margin: 15px 0; }
    .order-item { border-bottom: 1px solid #eee; padding: 10px 0; }
    .order-item:last-child { border-bottom: none; }
    .total { font-size: 18px; font-weight: bold; text-align: right; margin-top: 20px; padding-top: 10px; border-top: 2px solid #28a745; }
    .footer { text-align: center; padding: 20px; color: #666; font-size: 12px; }
    .success-icon { font-size: 48px; color: #28a745; text-align: center; margin: 10px 0; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Payment Successful!</h1>
    </div>
    
    <div class="content">
      <div class="success-icon">✓</div>
      <h2 style="text-align: center; color: #28a745;">Payment Received</h2>
      <p style="text-align: center;">Thank you for your payment, <%= @user.name %>!</p>
      
      <div class="receipt-box">
        <h3>Payment Receipt</h3>
        
        <div class="payment-info">
          <p><strong>Order Number:</strong> <%= @order.order_number %></p>
          <p><strong>Payment Date:</strong> <%= Time.now.strftime("%B %d, %Y at %I:%M %p") %></p>
          <p><strong>Amount Paid:</strong> ₹<%= sprintf('%.2f', @order.total_amount) %></p>
          <p><strong>Payment Method:</strong> Credit/Debit Card via Stripe</p>
          <% if @order.stripe_payment_intent_id.present? %>
            <p><strong>Transaction ID:</strong> <%= @order.stripe_payment_intent_id %></p>
          <% end %>
        </div>
        
        <h4 style="margin-top: 20px;">Order Summary</h4>
        <% @order.order_items.each do |item| %>
          <div class="order-item">
            <strong><%= item.product_name %></strong>
            <% if item.variant_name.present? %>
              <span style="color: #666;"> - Size: <%= item.variant_name %></span>
            <% end %>
            <br>
            <span>Quantity: <%= item.quantity %> × ₹<%= sprintf('%.2f', item.price) %> = ₹<%= sprintf('%.2f', item.subtotal) %></span>
          </div>
        <% end %>
        
        <div class="total">
          Total Paid: ₹<%= sprintf('%.2f', @order.total_amount) %>
        </div>
      </div>
      
      <h4>Shipping To:</h4>
      <p>
        <%= @order.shipping_name %><br>
        <%= @order.shipping_address %><br>
        <%= @order.shipping_city %>, <%= @order.shipping_state %> <%= @order.shipping_zip %><br>
        <%= @order.shipping_country %>
      </p>
      
      <p style="margin-top: 20px;">Your order will be processed and shipped within 2-3 business days.</p>
      
      <center style="margin: 30px 0;">
        <a href="<%= ENV['MAILER_HOST'] || 'http://localhost:3000' %>/orders/<%= @order.id %>" style="display: inline-block; padding: 12px 24px; background-color: #28a745; color: white; text-decoration: none; border-radius: 5px;">Track Your Order</a>
      </center>
    </div>
    
    <div class="footer">
      <p>Keep this email for your records.</p>
      <p>If you have any questions, please contact our support team.</p>
      <p>This is an automated email, please do not reply.</p>
    </div>
  </div>
</body>
</html>
